# --- Secrets / local config (NEVER commit) --------------------------------
.env
*.env
!.env.example
.claude/                 # contains real pod IPs/ports in settings.local.json
.vscode/

# --- Python ----------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# --- Runtime data / generated (large, machine-specific) --------------------
ollama_models/
chroma_db/
local_db/
uploads/
open-webui/
*.db                     # webui.db, chroma sqlite, etc.
*.sqlite3
*.log

# --- OS / editor noise -----------------------------------------------------
.DS_Store
Thumbs.db
*.swp

# --- Sample/working artifacts (keep the repo lean) -------------------------
# Uncomment if you don't want the sample P&ID image tracked:
# *.jpg
# *.png
